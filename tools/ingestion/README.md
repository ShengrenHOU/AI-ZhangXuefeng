# Ingestion Tools

These scripts support the offline knowledge lane.

- `normalize_sources.py`: normalize raw source files into repository JSON shape
- `promote_version.py`: copy a reviewed bundle into the published tree

Phase 1 keeps these scripts intentionally small so the online MVP is not blocked by the offline lane.

